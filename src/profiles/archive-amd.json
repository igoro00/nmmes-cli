{
    "options": {
        "modules": ["encoder", "normalize"],
        "encoder-quality": 20,
        "encoder-preset": "fast",
        "normalize-scale": 720,
        "encoder-video-codec": "libx265"
    }
    "messages": [{
        "level": "info",
        "text": "This profile should only be used with compatible AMD GPU card."
    }]
}
